# Extended Kalman Filter — C implementation

Generic EKF engine in C applied to drone attitude and position estimation
via IMU/GPS sensor fusion.

## Status
Work in progress — matrix library complete, EKF core in progress.

## Structure
src/        — C source files
include/    — headers
tests/      — unit tests


## Build
make        — build main simulation
make test   — run unit tests
